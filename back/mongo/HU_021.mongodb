
// BD name
const database = 'mt-c4-db';

// The current database to use.
use(database);

// Advances collection
collection = 'advances';

const student = db.users.findOne({email: 'peter.griffin@mail.com'})._id;
//db.getCollection(collection).find();
db.getCollection(collection).find({student_id : student});